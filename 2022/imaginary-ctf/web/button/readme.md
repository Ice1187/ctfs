1. Use the following code to list all the functions in the website.

   ```javascript
   for (var b in window) {
     if (window.hasOwnProperty(b)) console.log(b);
   }
   ```

2. There is a `motSusfunclion` function, so call it and get the flag.
